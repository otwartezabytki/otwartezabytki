%section.documents
  %h2 Dokumenty urzędowe
  - if relic.documents.count > 0
    = link_to 'edytuj', edit_section_relic_path(relic.id, :documents), :title => 'edytuj dokumenty urzędowe zabytku', :remote => true
    .content
      - relic.documents.each do |document|
        .document{ :class => document.mime_class }
          %h3= link_to document.name, document.file.url
          %p
            dodano:
            = l document.created_at.to_date, :format => :short
          %p
            rozmiar:
            = (document.size / 1024 / 1024).round(1)
            MB
  - else
    .placeholder
      %p Nikt jeszcze nie wgrał żadnego pliku. Bądź pierwszy.
      = link_to "wgraj plik", edit_section_relic_path(relic.id, :documents), :remote => true