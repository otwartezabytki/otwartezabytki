%section.show.links
  %h2 Linki, źródła, powiązania
  - if relic.all_links.count > 0
    = link_to 'edytuj', edit_section_relic_path(relic, :links), :title => 'edytuj linki powiązane z zabytkiem', :remote => true, :class => "edit-relic-link"
    .content
      %ul
        - relic.all_links.each do |link|
          %li
            = link_to link.shortened_url, link.url
            \-
            = link.name
  - else
    .placeholder
      %p
        Nikt jeszcze nie dodał żadnych linków.
        %br
        Bądź pierwszy!

      = link_to "dodaj link", edit_section_relic_path(relic, :links), :remote => true