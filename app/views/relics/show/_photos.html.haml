- if relic.has_photos?
  %section.show.photos
    .content
      .actions
        = link_to t('views.relics.show.photos.show', :amount => relic.valid_photos.count), relic_photo_path(relic.to_param, relic.valid_photos.first.id), :class => "show-photos", :remote => true
        = link_to t('views.relics.show.photos.add'), edit_section_relic_path(relic, :section => :photos), :class => "edit-photos", :remote => true
      = link_to image_tag(relic.main_photo.file.maxi.url, :width => 300, :height => 300, :alt => relic.main_photo.alternate_text) + content_tag(:span, relic.main_photo.author), relic_photo_path(relic, relic.main_photo.id), :class => "main-photo", :remote => true
      - if relic.all_photos.count > 0
        %ul#slider_mini.jcarousel-skin-mini{ :"data-photos" => relic.valid_photos.all.to_json }
- else
  %section.show.photos.placeholder
    .content
      .ico
      = link_to t('views.relics.show.photos.add'), edit_section_relic_path(relic.to_param, :section => :photos), :class => "edit-photos", :remote => true
