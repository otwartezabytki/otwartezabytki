.hello
  .about_top
    Witaj!
  .box
    %h3 Witaj, <strong>#{current_user.username}</strong>
    %p
      - if params[:notification] == 'password'
        Hasło do Twojego konta zostało zmienione.
      - else
        Wysłaliśmy Ci hasło na podany przez Ciebie adres e-mail.
        Będzie Ci potrzebne do zalogowania się przy ponownej wizycie.

    = link_to root_path, :class => "btn" do
      Przejdź do wyszukiwania zabytków &raquo;