(function(){
  var width = <%= @config[:width].to_i %>;
  var height = <%= @config[:height].to_i %>;

  document.write("Hello from js! width is set to " + width + " and height is set to " + height);
})()
